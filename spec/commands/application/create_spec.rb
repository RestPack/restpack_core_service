require 'spec_helper'

describe Commands::Core::Application::Create do
  # let(:response) { p "SS:#{subject}";subject.class.run(params) }

  # it_acts_as_create_command(:core, :application)



  pending 'validate command inputs'
end
